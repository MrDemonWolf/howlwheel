# Public overlay Worker (apps/server) — env for local `alchemy dev`.

# Origin allowed to call the overlay API (your web app). In production this
# defaults to the deployed web URL; for local dev point it at the web dev server.
CORS_ORIGIN=http://localhost:3001
